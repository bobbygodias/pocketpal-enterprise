buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        // Forty-Two AI's Vulkan backend uses Vulkan 1.1-era entry points such as
        // vkGetPhysicalDeviceFeatures2. Android's NDK Vulkan stub exports these
        // symbols starting at API 28. The target MEGA 3 runs API 35, so keeping
        // the application floor at 28 lets the native linker resolve Vulkan
        // correctly without bundling or replacing Android's system libvulkan.
        minSdkVersion = 28
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.3.13750724"
        kotlinVersion = "2.1.20"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")

        // Add the Google Services plugin dependency
        // This is used exclusively for sending benchmarks (with user consent) to Hugging Face Spaces via Firebase.
        // Firebase is used for App Check functionality, allowing unauthenticated users to submit their benchmark data securely.
        classpath 'com.google.gms:google-services:4.4.4'
    }
}

apply plugin: "com.facebook.react.rootproject"
